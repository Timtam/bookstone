from typing import Tuple

from .tag import Tag

Tags: Tuple[Tag, ...] = (
    Tag("author", "Unknown Author"),
    Tag("title", "Unknown Title"),
    Tag("series", "Unknown Series"),
    Tag("number", "00"),
)
